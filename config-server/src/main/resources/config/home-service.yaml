spring:
  data:
    mongodb:
      uri: mongodb://admin:password123@localhost:27019/home-service-db?authSource=admin
      auto-index-creation: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8091