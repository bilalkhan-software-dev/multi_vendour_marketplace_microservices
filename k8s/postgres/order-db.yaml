apiVersion: v1
kind: PersistentVolume
metadata:
  name: order-db-postgres-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: "/data/order-db-postgres"
    type: DirectoryOrCreate

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: order-db-postgres-pvc
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
  accessModes:
    - ReadWriteOnce

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: order-db-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-db-postgres
  serviceName: order-db-postgres-headless
  template:
    metadata:
      labels:
        app: order-db-postgres
    spec:
      containers:
        - name: order-db-postgres
          image: postgres:15
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: password

            - name: POSTGRES_DB
              value: "order-service-db"
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: order-db-postgres-storage
              mountPath: /var/lib/postgresql/data  # ← stores data here
      volumes:
        - name: order-db-postgres-storage
          persistentVolumeClaim:
            claimName: order-db-postgres-pvc  # ← Points to PVC

---
apiVersion: v1
kind: Service
metadata:
  name: order-db-postgres-headless
spec:
  clusterIP: None
  selector:
    app: order-db-postgres
  ports:
    - port: 5432
      targetPort: 5432

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: order-db-cm
data:
  hostname: "order-db-postgres-0.order-db-postgres-headless"


