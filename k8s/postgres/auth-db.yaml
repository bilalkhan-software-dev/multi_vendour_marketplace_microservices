apiVersion: v1
kind: PersistentVolume
metadata:
  name: auth-db-postgres-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: "/data/auth-db-postgres"
    type: DirectoryOrCreate

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-db-postgres-pvc
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
  accessModes:
    - ReadWriteOnce

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auth-db-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-db-postgres
  serviceName: auth-db-postgres-headless
  template:
    metadata:
      labels:
        app: auth-db-postgres
    spec:
      containers:
        - name: auth-db-postgres
          image: postgres:15
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: password

            - name: POSTGRES_DB
              value: "auth-service-db"
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: auth-db-postgres-storage
              mountPath: /var/lib/postgresql/data  # ← stores data here
      volumes:
        - name: auth-db-postgres-storage
          persistentVolumeClaim:
            claimName: auth-db-postgres-pvc  # ← Points to PVC

---
apiVersion: v1
kind: Service
metadata:
  name: auth-db-postgres-headless
spec:
  clusterIP: None
  selector:
    app: auth-db-postgres
  ports:
    - port: 5434
      targetPort: 5432

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-db-cm
data:
  hostname: "auth-db-postgres-0.auth-db-postgres-headless"


