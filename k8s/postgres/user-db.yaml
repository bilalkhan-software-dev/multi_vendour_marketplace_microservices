apiVersion: v1
kind: PersistentVolume
metadata:
  name: user-db-postgres-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: "/data/user-db-postgres"
    type: DirectoryOrCreate

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: user-db-postgres-pvc
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
  accessModes:
    - ReadWriteOnce

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-db-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-db-postgres
  serviceName: user-db-postgres-headless
  template:
    metadata:
      labels:
        app: user-db-postgres
    spec:
      containers:
        - name: user-db-postgres
          image: postgres:15
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: password

            - name: POSTGRES_DB
              value: "user-service-db"
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: user-db-postgres-storage
              mountPath: /var/lib/postgresql/data  # ← stores data here
      volumes:
        - name: user-db-postgres-storage
          persistentVolumeClaim:
            claimName: user-db-postgres-pvc  # ← Points to PVC

---
apiVersion: v1
kind: Service
metadata:
  name: user-db-postgres-headless
spec:
  clusterIP: None
  selector:
    app: user-db-postgres
  ports:
    - port: 5432
      targetPort: 5432

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-db-cm
data:
  hostname: "user-db-postgres-0.user-db-postgres-headless"
