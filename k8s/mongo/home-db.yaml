apiVersion: v1
kind: PersistentVolume
metadata:
  name: home-db-mongo-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: "/data/db"
    type: DirectoryOrCreate

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-db-mongo-pvc
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
  accessModes:
    - ReadWriteOnce

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: home-db-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: home-db-mongo
  serviceName: home-db-mongo-headless
  template:
    metadata:
      labels:
        app: home-db-mongo
    spec:
      containers:
        - name: home-db-mongo
          image: mongo:8.0
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: username
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: password
            - name: MONGO_INITDB_DATABASE
              value: "home-service-db"
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: home-db-mongo-storage
              mountPath: /var/lib/mongo/data  # ← stores data here
      volumes:
        - name: home-db-mongo-storage
          persistentVolumeClaim:
            claimName: home-db-mongo-pvc  # ← Points to PVC

---
apiVersion: v1
kind: Service
metadata:
  name: home-db-mongo-headless
spec:
  clusterIP: None
  selector:
    app: home-db-mongo
  ports:
    - port: 27017
      targetPort: 27017

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: home-db-cm
data:
  mongodb_url: "mongodb://admin:password123@home-db-mongo-0.home-db-mongo-headless:27017/home-service-db?authSource=admin"
